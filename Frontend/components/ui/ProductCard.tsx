// FILE: components/ui/ProductCard.tsx
// Placeholder — full implementation coming in Prompt 2

export default function ProductCard() {
  return (
    <div
      style={{
        background: 'var(--surface)',
        border: '1px solid var(--border)',
        borderRadius: 'var(--radius)',
        padding: 24,
        minHeight: 300,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <span style={{ color: 'var(--muted)', fontSize: 12, fontFamily: 'var(--font-dm)' }}>
        Product Card — Prompt 2
      </span>
    </div>
  )
}
